component extends=testbox.system.BaseSpec {

    function run() {
        describe("Tests HTTP status codes from responses", () => {
            it("handles 200-OK", () => {
            })
        })
    }
}
